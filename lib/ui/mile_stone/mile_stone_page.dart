import 'package:flutter/material.dart';

class MileStonePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('MileStone'),
      ),
      body: Text('Mile Stone Body'),
    );
  }
}
